const setupScript= require('./lib/setup')
const addStagingScript=require('./lib/add')
const commitScript=require('./lib/commit')

module.exports={
    addStagingScript,
    setupScript,
    commitScript
}